<?xml-stylesheet href="slideStyle.xml" type="text/xsl"?>
<slide num="6" title="Predicates and functions">
  <bl>
    <bi>Predicate, evaluated with each node as context node
      <example>foo[bar]
descendant::a[@href]
link[@rel="stylesheet"]</example>
    </bi>
    <bi>NodeSet functions: id("header"), count(users), local-name(.), namespace-uri(..)</bi>
    <bi>String functions: concat, starts-with, contains, substring, string-length</bi>
    <bi>Boolean functions: not(@href), true(), false()</bi>
    <bi>Number functions: sum(employees/salary), round(@size div 1024)</bi>
  </bl>
</slide>
