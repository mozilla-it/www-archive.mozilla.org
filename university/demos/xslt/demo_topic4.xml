<?xml version="1.0"?>

<topic>
<title>The document() Method</title>

<p>The rule that actually associates the filename values listed in the
doc_set with the actual files on disk is the following: </p>

<code>&lt;xsl:for-each select="/doc/item"&gt;
   &lt;xsl:apply-templates select="document(string(@filename))/topic"/&gt;
&lt;/xsl:for-each&gt;
</code>

</topic>

